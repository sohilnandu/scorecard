<%= simple_form_for(@scorecard) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :site_id %>
    <%= f.input :org_name %>
    <%= f.input :data %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
